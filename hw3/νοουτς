public syncronized μπλα μπλα read() {
    ...λογικη διαβασματος 
    ωχ! τελειωσε ότι διαβασα και πρεπει να περιμενω
    wait(cond)
   
}
 public sync write() {
    ...λογικη γραψιματος
    signal(cond)
 }

 οι συναρτησεις που ειναι σε sync εκτελουνται ατομικάαααααααααααα

 